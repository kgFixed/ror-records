# This is a basic workflow to help you get started with Actions

name: get release dats

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a set of commands using the runners shell
      - name: Debug v1.56 specifically
        run: |
          echo "🔍 DEBUG SPECIFIQUE pour v1.56"
          echo "==============================="
          
          version="v1.56"
          
          # 1. Test de l'URL exacte
          echo "1. URL testée:"
          echo "https://api.github.com/repos/ror-community/ror-updates/releases/tags/$version"
          echo ""
          
          # 2. Requête complète avec tous les headers
          echo "2. Réponse complète:"
          curl -i -s "https://api.github.com/repos/ror-community/ror-updates/releases/tags/$version"
          echo ""
          
          # 3. Juste le body
          echo "3. Body seulement:"
          curl -s "https://api.github.com/repos/ror-community/ror-updates/releases/tags/$version"
          echo ""
          
          # 4. Test avec un user-agent
          echo "4. Avec User-Agent:"
          curl -s -H "User-Agent: MyApp" "https://api.github.com/repos/ror-community/ror-updates/releases/tags/$version"
          echo ""
